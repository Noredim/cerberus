# dashboard-grids

Esta task contém as descrições e o controle de implementação da conversão das telas em blocos (cards) para telas em GRID (Table data grid) no sistema, especificamente:

- RolesDashboard.tsx
- ProfessionalsDashboard.tsx
